# API package — local-only artifacts
node_modules/
dist/
*.log
.env
.env.*
!.env.example
# Local NDJSON audit sink (never commit — may contain PII references)
audit-*.ndjson
# Generated narration assets + manifest — these go to object storage / a prod
# volume (AUDIO_DIR + AUDIO_PUBLIC_BASE), never into git (large, regenerable).
data/audio/
data/audio-content.json
