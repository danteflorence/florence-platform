# dependencies
node_modules/

# build output
dist/
dist-ssr/

# logs
*.log
npm-debug.log*

# editor / OS
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea/

# env / secrets — never commit
.env
.env.*
!.env.example
!.env.*.example
*.local

# scratch
/tmp/

# typescript incremental build cache
*.tsbuildinfo

# local editor / preview tool config (contains machine-specific absolute paths;
# the Preview MCP reads the global ~/.claude/launch.json, not this one)
.claude/
