# Core Admin Design System TODO

## Current First Pass

Core admin is server-rendered from the Core API, so this pass documents the migration boundary instead of reshaping the admin HTML.

## Next Migration Steps

- Add the shared Florence tokens to the Core admin HTML shell.
- Replace local button, card, table, and alert styles with design-system-equivalent classes.
- Use `LegalDisclaimer` copy patterns on high-sensitivity identity, document vault, consent, audit, AI governance, and Production Ledger surfaces.
- Preserve tenant scope, audit logging, signed URL, consent, AI review, and fail-closed gates while changing visual presentation.
